import {useTranslation} from "react-i18next";import React from "react";import LoadingText from "./LoadingText";const Placeholder = ({ dataReady }) => {    const { t } = useTranslation();    return (        <div className="h-100 p-d-flex p-jc-center p-ai-center">            {dataReady ? (                <div className="p-d-flex p-flex-column p-jc-center p-ai-center text-white">                    <i className="pi pi-user-edit" style={{ fontSize: "6em" }} />                    <h3 className="p-mt-3">{t("unfinishedChecksPanel.placeholder_text")}</h3>                </div>            ) : (                <LoadingText                    text={t("unfinishedChecksPanel.placeholder_searching_text")}                    spinnerColor={"#8D249899"}                />            )}        </div>    );};export default Placeholder